# Example production env file for Vite (do NOT store private keys here)
# Copy to `.env.production` locally or set VITE_ vars in Vercel project settings.

# RPC endpoint exposed to the frontend (optional)
VITE_RPC_URL=https://mainnet.base.org

# Other VITE_ prefixed variables you need for the browser UI
# VITE_FEATURE_FLAG=true
